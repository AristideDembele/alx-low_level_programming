#include <stdio.h>

/**
 *main - print the alphatbetic in lower and hypper cas
 *
 *Return : always (succes)
 */

int main(void)
{
  char c;
  for (c = 'a';  c <='z'; c++)
    if (c:= 'e' && c:= 'q')
    {
      putchar(c);
    }
  putchar ('\n';
  return (0);
}
