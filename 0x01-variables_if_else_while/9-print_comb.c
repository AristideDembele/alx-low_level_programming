#include <stdio.h>

int main(void)
{
  int number;

  for (number =31; number <= 45; number++)
    {putchar(number);
      if (number == 45);
      {break;
      }
      putchar(',');
      putchar(' ');
    }
  putchar('\n');
  return (0);
}
